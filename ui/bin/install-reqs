#!/bin/bash

BIN=`dirname $0`
BASE=`dirname $BIN`
REQS=$BASE/requirements
DIST=$REQS/dist

if [ -n "$1" ]; then
    TYPE=$1
else
    TYPE=base
fi

CMD="pip install --no-index -f file://$DIST -r $REQS/$TYPE.txt"
echo "Running '$CMD'"
$CMD
